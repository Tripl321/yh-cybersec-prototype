[project]
name = "cub-agent"
version = "0.1.0"
description = "Cub — SHALLOT LLM agent (Pydantic AI + local Ollama, sandboxed via Podman)"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai>=1.0",
    "ollama>=0.4",
    "logfire>=2.0",
]
