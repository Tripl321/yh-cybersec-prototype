[project]
name = "shallot-webauthn-demo"
version = "0.1.0"
description = "SHALLOT WebAuthn demo — Mama Bear / Cub auth prototype"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "webauthn>=2.0",
    "cryptography>=42.0",
]
